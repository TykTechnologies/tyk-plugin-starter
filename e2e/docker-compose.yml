services:
  redis:
    image: redis:7-alpine
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 1s
      timeout: 3s
      retries: 30

  bundle-server:
    image: python:3.12-alpine
    volumes:
      - ./bundles:/srv:ro
    working_dir: /srv
    command: python3 -m http.server 8500
    expose:
      - "8500"

  # Local httpbin (multi-arch go reimplementation, runs offline) — the apps
  # proxy here instead of the public httpbin.org so the suite is deterministic
  # and doesn't flake on that host being slow/rate-limited. Mirrors dev/.
  httpbin:
    image: mccutchen/go-httpbin:v2.15.0
    expose:
      - "8080"

  gateway:
    # Official Tyk OSS image with the goja JS engine. Pin to the current
    # pre-release; drop the -alphaN suffix (→ tykio/tyk-gateway:v5.15.0) after GA.
    image: ${TYK_IMAGE:-tykio/tyk-gateway:v5.15.0-alpha5}
    depends_on:
      redis:
        condition: service_healthy
      bundle-server:
        condition: service_started
      httpbin:
        condition: service_started
    volumes:
      - ./tyk.conf:/opt/tyk-gateway/tyk.conf:ro
      - ./apps:/opt/tyk-gateway/apps:ro
      - ./policies:/opt/tyk-gateway/policies:ro
    ports:
      - "18080:8080"
